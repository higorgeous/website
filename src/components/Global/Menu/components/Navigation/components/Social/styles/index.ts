import styled from '@emotion/styled';

export const Icon = styled.span`
  max-width: 25px;
  a {
    img {
      height: 25px;
      width: 100%;
      display: block;
      max-height: 25px;
      filter: invert(1);
    }
  }
`;
