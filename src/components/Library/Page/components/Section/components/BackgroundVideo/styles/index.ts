import styled from '@emotion/styled';

export const Video = styled.video`
  object-fit: cover;
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  height: 100vh;
  width: 100%;
  transform: translateY(0) !important;
`;
